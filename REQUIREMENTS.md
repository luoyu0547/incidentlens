# IncidentLens 需求文档

## 1. 项目概述

IncidentLens 是一个面向微服务故障调查的证据驱动 Agent Demo。

它不假装替代生产环境的 SRE 平台，也不执行真实生产变更。项目的目标是搭建一个可复现的微服务实验环境，验证 Agent 是否能够通过日志、指标、分布式链路和部署记录完成跨服务故障调查，并将已经确认的事故经验沉淀为可检索的组织记忆。

核心问题：

> 当故障信息分散在多个可观测数据源中时，Agent 能否基于工具调用和当前证据形成、验证并修正故障假设，而不是直接猜测根因？

## 2. 项目目标

### 2.1 必须实现

- 搭建可复现的微服务实验环境，至少包含网关、订单服务和支付服务。
- 支持通过配置或管理接口注入下游延迟、错误率、依赖不可用、连接池耗尽等故障。
- 真实产生并保存结构化日志、请求指标、调用链和部署记录。
- 提供受限的只读查询工具，包括指标查询、日志检索、Trace 查询、服务依赖查询和部署记录查询。
- 实现有状态的故障调查 Agent，维护当前症状、候选假设、证据、已排除路径和调查轮次。
- Agent 必须通过工具获得证据，不能直接读取故障场景的根因标签。
- 生成带证据引用的根因报告、置信度、无效假设和处置建议。
- 将经过人工确认的事故沉淀为结构化历史案例。
- 使用历史案例为后续调查生成候选假设，但必须用当前遥测数据重新验证历史结论。
- 提供可复现故障场景和 baseline 对照评测。
- 提供本地 Web 页面查看告警、调查轨迹、工具调用、假设、证据链、历史案例和最终报告。
- 使用 Docker Compose 提供一键启动方式。

### 2.2 明确不实现

- 不连接真实企业生产环境。
- 不执行真实生产重启、回滚、配置修改或数据库写操作。
- 不声称降低了真实企业的 MTTR。
- 不实现 Kubernetes 集群、A2A、多 Agent 协作或自动修复。
- 不引入复杂的企业权限、多租户和 OAuth 系统。
- 不把静态 JSON 直接伪装成实时可观测数据。

## 3. 目标用户与使用场景

### 3.1 目标用户

- 后端工程师：需要快速理解一次跨服务故障的影响范围和可能根因。
- SRE/平台工程师：希望将排障过程和事故经验沉淀为可复用案例。
- 面试评审者：需要通过可复现的实验验证 Agent 的工具使用、状态管理、证据绑定和评测能力。

### 3.2 核心场景

系统收到告警：

```text
order-service P95 延迟升高，5xx 错误率达到 17%。
```

Agent 应当：

1. 确定故障时间窗口和受影响服务；
2. 检索历史相似事故，生成候选调查方向；
3. 查询指标、日志、调用链、服务依赖和部署记录；
4. 将查询结果转换为结构化 Evidence；
5. 更新、支持或排除候选 Hypothesis；
6. 在证据充分时生成根因报告；
7. 在证据不足或冲突时明确说明不确定性；
8. 经人工确认后，将事故总结写入组织记忆。

## 4. 设计原则

### 4.1 证据优先

根因结论必须引用 Evidence ID。没有当前证据支持时，Agent 不得输出高置信度结论。

### 4.2 历史经验是先验，不是答案

RAG 检索结果只能帮助 Agent 形成候选假设。历史案例中的根因必须通过当前日志、指标、Trace 和部署记录重新验证。

### 4.3 真实运行、可复现故障

故障需要通过真实服务调用产生影响，并能够重复开启、关闭和重置。Agent 只能访问观测结果，不能访问场景内部的根因标签。

### 4.4 只读安全边界

第一版所有外部工具均为只读查询。高风险处置只展示建议和审批状态，不执行真实变更。

### 4.5 过程可审计

记录模型调用、状态迁移、Skill/策略选择、工具参数、工具结果摘要、证据变化、重试和最终报告。

## 5. 功能需求

### FR-01 微服务实验环境

- 至少提供 `gateway-service`、`order-service`、`payment-service`。
- 服务之间通过 HTTP 调用并传递 request ID、trace ID。
- 服务输出统一 JSON 结构化日志。
- 每个服务提供健康检查接口。
- 支持创建真实业务请求，以产生正常和异常遥测数据。

### FR-02 故障注入

至少支持以下故障类型：

- `payment_delay`：支付服务增加可配置延迟；
- `payment_error_rate`：支付服务按比例返回错误；
- `db_pool_exhaustion`：订单服务使用受限连接池并产生获取连接超时；
- `dependency_unavailable`：下游依赖不可用；
- `deployment_regression`：记录故障前后的配置或版本变化。

每个场景必须支持启用、禁用和环境重置，并保存场景参数以便评测复现。

### FR-03 可观测数据

系统至少保存以下数据：

- Metrics：请求数、错误率、延迟分位数、下游调用耗时、连接池使用情况；
- Logs：时间、服务、级别、trace ID、错误类型、消息和请求上下文；
- Traces：调用链、span、服务名称、耗时、状态和父子关系；
- Deployments：服务、版本、发布时间、配置差异和操作者。

原始数据必须能够通过工具按服务、时间窗口、关键字、Trace ID 或最小耗时查询。

### FR-04 Agent 调查流程

Agent 采用有状态调查循环：

```text
parse_alert
  -> scope_incident
  -> retrieve_memory
  -> generate_hypotheses
  -> choose_next_action
  -> execute_tool
  -> record_evidence
  -> update_hypotheses
  -> verify_root_cause
  -> generate_report
```

调查状态至少包含：

- incident ID；
- 告警症状和时间窗口；
- 受影响服务；
- 当前假设、支持证据和反驳证据；
- 已调用工具及摘要；
- 已加载的排障策略；
- 已排除假设；
- 当前调查轮次、状态和置信度；
- 最终根因和报告。

Agent 必须能够：

- 根据证据选择下一步工具；
- 处理空结果、超时、重复调用和工具错误；
- 在证据冲突时降低置信度或创建新假设；
- 在达到最大轮次后停止并说明原因；
- 从已保存检查点恢复调查。

### FR-05 工具接口

第一版提供以下只读工具：

- `query_metrics(service, metric, start_time, end_time)`；
- `search_logs(service, keyword, start_time, end_time, trace_id)`；
- `get_slow_traces(service, min_duration_ms, start_time, end_time)`；
- `get_trace(trace_id)`；
- `get_service_dependencies(service)`；
- `list_recent_deployments(service, start_time, end_time)`；
- `get_runbook(service, incident_type)`。

所有工具必须具备：

- Pydantic 参数校验；
- 超时和有限重试；
- 结果条数和文本长度限制；
- 统一成功/失败返回结构；
- 工具调用审计记录；
- 禁止写操作的权限声明。

### FR-06 事故记忆与 RAG

事故案例至少包含：

- 症状；
- 受影响服务；
- 根因类别和描述；
- 关键证据；
- 排查路径；
- 无效假设；
- 解决方案和处置建议；
- 适用条件和不适用条件；
- 服务版本、环境和时间信息；
- 验证状态、反馈和更新时间。

案例状态包括：

```text
draft
agent_generated
human_verified
deprecated
rejected
```

检索要求：

- 优先检索 `human_verified` 案例；
- 支持症状、服务、故障类别和版本过滤；
- 支持关键词匹配与语义相似度扩展；
- 相同症状但根因不同的案例不能被简单合并；
- 历史案例必须展示来源和相似理由；
- 检索结果必须能追溯到当前调查中的候选假设。

### FR-07 事故确认与知识沉淀

- Agent 完成报告后进入待确认状态。
- 用户可以确认、修改、驳回根因及解决方案。
- 只有确认后的案例才进入正式检索集合。
- 支持标记历史案例“有帮助、部分有帮助、无关、过时或错误”。
- 记录案例被召回、被采用或误导的情况。

### FR-08 Web 页面

页面至少展示：

- 当前告警及故障注入状态；
- 调查进度和时间线；
- Agent 状态和当前假设；
- 工具调用名称、参数摘要和结果摘要；
- 证据列表及其支持/反驳的假设；
- 历史案例召回结果；
- 根因、置信度、处置建议和证据引用；
- 确认、驳回和案例反馈操作。

页面重点展示可验证事实，不展示未经核验的模型“思考过程”。

### FR-09 评测

至少提供 5 类可复现故障和对应测试任务，比较：

1. 无历史记忆的普通 ReAct Agent；
2. 使用历史案例但不重新验证的 Agent；
3. 使用历史案例并结合当前证据验证的 IncidentLens Agent。

记录以下指标：

- 根因服务识别准确率；
- 根因类型识别准确率；
- 证据引用正确率；
- 首次出现有效假设的轮次；
- 平均工具调用次数；
- 重复工具调用率；
- 历史案例误导率；
- 平均调查耗时。

评测结果必须由实际运行产生，不允许手写固定成功率。

## 6. 非功能需求

### NFR-01 可复现性

- 新环境可以通过 Docker Compose 启动；
- 提供示例故障场景、种子数据和重置命令；
- 同一场景在相同配置下应得到可比较的结果。

### NFR-02 可解释性

- 每个根因结论必须关联证据；
- 每个证据必须关联具体工具调用；
- 历史案例必须显示来源、状态和版本范围。

### NFR-03 安全性

- 默认只允许查询工具；
- 不允许写 SQL、删除数据、关闭监控或修改生产配置；
- 工具参数经过校验；
- 限制查询时间范围、返回数量和响应大小。

### NFR-04 可恢复性

- 重要状态在每轮工具调用后持久化；
- 单个工具失败不应导致整个任务丢失；
- 任务可以从最近检查点继续。

### NFR-05 可观测性

- 记录 Agent 调查轨迹、工具调用、状态迁移、错误和耗时；
- 能够导出一次调查的完整 JSON 记录用于复盘和评测。

## 7. 推荐技术方向

第一版推荐使用：

- Python、FastAPI、Pydantic；
- LangGraph 或等价的显式状态机；
- SQLite 作为本地持久化存储；
- Docker Compose；
- OpenTelemetry 数据模型或兼容的轻量遥测接收层；
- SSE 推送调查进度；
- pytest 作为场景和评测框架；
- 可选的 OpenAI-compatible 模型适配器；
- SQLite FTS/关键词检索作为 MVP 记忆检索，向量检索作为后续扩展。

## 8. 验收标准

项目达到以下条件才算完成 MVP：

1. 一条命令可以启动本地实验环境；
2. 用户可以开启一个故障并产生真实跨服务请求；
3. 系统能够查询对应的日志、指标和 Trace；
4. Agent 能通过至少两次工具调用形成并更新假设；
5. 根因报告能引用当前调查证据；
6. 历史事故可以经确认后被后续调查检索；
7. 检索出的历史根因会被当前遥测数据重新验证；
8. 至少一个错误历史案例能够被 Agent 排除；
9. 评测脚本能够输出真实指标；
10. 页面能够展示完整调查轨迹和证据链；
11. README 能说明项目边界、启动方式、故障场景和评测方式；
12. 不宣称已经解决真实企业生产故障或产生真实业务收益。

## 9. 项目边界说明

IncidentLens 是一个可复现的 Agent 工程实验项目，不是完整的生产级 AIOps 产品。它重点验证：

- Agent 如何管理长任务状态；
- Agent 如何设计和使用工具；
- Agent 如何将多源观测数据组织为证据；
- Agent 如何利用长期记忆而避免盲目复用历史结论；
- Agent 如何在受限权限下工作；
- Agent 如何通过基线和客观指标进行评测。

## 10. 简历表述建议

项目名称：

> IncidentLens——具备组织记忆的微服务故障诊断 Agent

建议表述：

> 搭建可复现微服务故障实验环境，设计基于状态机的诊断 Agent，接入日志、指标、分布式链路及部署记录，通过“历史案例检索—候选假设生成—当前证据验证”完成跨服务故障调查；将人工确认的事故根因、排查路径和处置方案沉淀为可检索组织记忆，并通过可复现故障场景评测根因识别率、证据引用正确率和历史案例误导率。

