"""Authentication test package.

The ``__init__.py`` marks this directory as a package so ``auth.helpers`` is
importable from sibling test packages (e.g. ``tests/api_v1``) when ``tests/``
is pytest's source root.
"""
