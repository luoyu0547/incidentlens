// Minimal DOM type declarations required by the auto-generated Fetch client.
// BodyInit is used in type assertions within the generated SDK code.
// In browser environments this comes from the DOM lib; in Node.js 18+
// the global fetch implementation handles these types at runtime.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
type BodyInit = any;
