/**
 * OverviewPage placeholder.
 *
 * Task 3 only establishes routing and the app shell; the full overview
 * implementation lands in a later task. This placeholder still asserts the
 * route is reachable from navigation with its Chinese title.
 */
export function OverviewPage() {
  return <h2>总览</h2>;
}
